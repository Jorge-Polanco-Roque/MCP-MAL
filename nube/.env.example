# --- Modo de infraestructura ---
INFRA_MODE=gcp            # "gcp"
TRANSPORT=http            # "http" (Cloud Run siempre HTTP)
PORT=3000

# --- GCP ---
FIRESTORE_PROJECT=mal-dev-xxxxxx
GCS_BUCKET=mal-mcp-assets-mal-dev-xxxxxx
GCP_PROJECT_ID=mal-dev-xxxxxx

# --- Auth ---
# API keys se gestionan via Secret Manager

# --- MCPs downstream (tokens en Secret Manager) ---
# GITHUB_TOKEN=ghp_xxxxx
# SLACK_TOKEN=xoxb-xxxxx
