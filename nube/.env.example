# === Required ===
FIRESTORE_PROJECT=your-gcp-project-id
GCS_BUCKET=mal-mcp-assets-your-project-id
GCP_PROJECT_ID=your-gcp-project-id

# === Transport ===
TRANSPORT=http                    # http | stdio (default: http)
PORT=3000                         # HTTP port (default: 3000)
HOST=0.0.0.0                     # Bind address (default: 0.0.0.0)
INFRA_MODE=gcp                   # Always "gcp" for this deployment path

# === Firestore ===
FIRESTORE_DATABASE_ID=mal-catalog # Firestore database name (default: mal-catalog)

# === Sessions ===
SESSION_TIMEOUT_MS=1800000        # Idle session timeout in ms (default: 1800000 = 30 min)
MAX_SESSIONS=100                  # Max concurrent MCP sessions (default: 100)

# === Security ===
# API_KEY is stored in Secret Manager, not here.
# For local dev with emulators, set directly:
# API_KEY=dev-key-123
CORS_ORIGINS=                     # Comma-separated allowed origins (empty = same-origin only)

# === Secret Manager ===
SECRET_CACHE_TTL_MS=300000        # Secret cache TTL in ms (default: 300000 = 5 min)

# === GCS ===
GCS_MAX_FILE_SIZE=10485760        # Max file read size in bytes (default: 10485760 = 10MB)

# === Logging ===
LOG_LEVEL=info                    # pino log level: debug | info | warn | error
NODE_ENV=production               # production | development

# === Emulators (local dev only) ===
# FIRESTORE_EMULATOR_HOST=localhost:8080
# STORAGE_EMULATOR_HOST=http://localhost:4443
